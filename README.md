# HOVI API Server :hospital"

## HOVI의 api 서버입니다.

사용한 라이브러리

- cors (cors 허용)
- dotenv (env 파일)
- express 
- helmet (보안)
- pino (빠른 로깅)
- body-parser
- nodemon (계속 서버를 띄우기 위함)

## HOW TO START
- npm install -g yarn (yarn 설치)
- yarn (패키지 설치)
- yarn dev (실행)
- yarn add "패키지명" (패키지 추가시)